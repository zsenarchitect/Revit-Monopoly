# this handle the revit display.
# include setting color in view, change filter.


def color_asset_by_agent(asset, agent):
    # change the display color of the asset to match the agent team color
    pass
